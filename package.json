{
  "name": "@iocky/lyricbook-workspace",
  "version": "0.0.1",
  "private": true,
  "description": "Local-first concert lyric book and printable booklet generator.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "lint": "node scripts/lint.mjs",
    "typecheck": "node scripts/typecheck.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:rust": "node scripts/test-rust.mjs",
    "validate:content": "node scripts/validate-content.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run lint && npm run typecheck && npm run test && npm run test:rust && npm run validate:content && npm run build",
    "test:e2e": "node scripts/e2e-help.mjs"
  }
}
