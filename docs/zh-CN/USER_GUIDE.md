# 用户指南

1. 选择内置预设、空白项目或导入文件。
2. 在左侧曲库选择歌曲；“编辑”可修改歌名、别名、标签、版本和语言轨道。
3. 歌单编辑器可选择歌单、上移／下移歌曲、添加可选歌曲、小节和备注。
4. 主题编辑器只修改安全的颜色、圆角和打印令牌。
5. 沉浸模式按当前歌单显示上一首／下一首；歌词底部可直接进入下一首。
6. 导出 `.lyricbook` 作为完整备份，也可导出 JSON 或主题。
7. 导入替换前会自动在浏览器中保留一份备份；导入失败不会改动当前项目。

歌词默认只保存在当前浏览器。清理网站数据前请导出备份。
