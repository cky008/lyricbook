# G.E.M. I AM GLORIA 2.0 — Shenzhen preparation and opening-night record

## Status

The active `gem-gloria-shenzhen-2026-preparation` setlist separates the current fixed formal program from optional live requests. Its `confirmed` status is grounded in the user's explicit confirmation of the current arrangement, recorded as a distinct `user-confirmation` source. It does not claim an official guarantee that every future date is immutable.

The dated `gem-gloria-shenzhen-2026-09-11-observed` setlist remains a separate 42-song opening-night record with the same id and song/cue order. The earlier `gem-gloria-core` prediction remains available under an explicitly archived title and `archive` status; it is not the current Shenzhen preparation list.

## Preparation classification

| Material | Preparation treatment | Evidence boundary |
| --- | --- | --- |
| Formal main program | Required; retain the opening-night order | Current consistency explicitly confirmed by the user |
| `自由的你`, `泡沫`, `天空没有极限` | Required encore, not random encore candidates | User confirms these three are fixed in the current program |
| `画`, `失真`, `让你知道` | Optional live-request examples from opening night | Their performance is observed on 11 September; their selection is not promised on every date |
| Earlier prediction/library-only songs | Archived reference, not automatically inserted | Retaining a library song does not make it a current performance selection |

The preparation list contains 42 entries: 39 required and three optional request examples. These are lyric-editing entries, including medley components and the still-provisional drum cue, not a claim that all 39 identities have equal evidential certainty. The request section and each request song are explicitly optional. Separate required sections before `自由的你` and before `泡沫`/`天空没有极限` ensure section-level optionality cannot hide the fixed closing songs. Printing without optional songs retains all three fixed encore songs in A4, A5, and booklet formats.

## Confirmed event facts

- G.E.M.'s official tour page lists Shenzhen Universiade Sports Centre Stadium and twelve Shenzhen dates between 11 September and 5 October 2026.
- G.E.M. Studio's official opening-night post identifies 11 September as the first of twelve Shenzhen shows and confirms the premiere of the unreleased album lead single `自由的你`.
- A Shenzhen Longgang Culture and Tourism Bureau reply independently confirms the venue, date range, and twelve-show total.

## User-provided performance evidence

The user supplied a 42-segment video chapter list, then a more complete pasted video page giving `05:01` for the final `天空没有极限` chapter. All 42 segments now have durations totalling `2:33:22`. The original excerpt and the supplementary page are retained as distinct sources. Durations belong to the supplied video chapters, not to canonical studio recordings. Combined entries therefore retain one shared duration rather than inventing per-song timings.

The user also supplied an on-site photo of the stadium's infrared-laser camera safety notice. It names selected laser cues, including `Kingdom Come` and `G.E.M./Walk on water`. This notice corroborates repertoire but is not a complete running order. The source images remain private and are not committed.

Two additional screenshots contain audience discussion about a fixed twelve-show list and changed encore choices. Those comments remain community context. The user's subsequent explicit confirmation supports the current preparation classification; it is not promoted to an official twelve-show promise. Encore and request headings clarify the preparation list only; the dated observation retains the chapter sequence without inventing original stage-section boundaries.

## Reconciliations and unresolved conflicts

- The video chapter labelled `敲鼓` (`01:41`) occupies the expected Part 5 position, while the on-site laser notice names `Kingdom Come`. The observed setlist provisionally maps the segment to `gem-kingdom` at confidence `0.70`, with the raw label preserved in the item note.
- The video chapters list only `Walk On Water` (`04:12`), while the on-site notice says `G.E.M./Walk on water`. The dated setlist therefore keeps `gem-water` as the observed song and preserves `G.E.M.` only in the unresolved cue note; it does not add a song that the video chapters do not list separately.
- `睡公主 + 爱你A.I.N.Y.2017`, `Where Did U Go + 等一个他`, and `All About U + Good to be Bad` are expanded into their constituent songs so users can attach lyrics to each title. The exact supplied labels and combined durations remain in notes and `source-matrix.json`.
- `点歌清唱：画 + 失真 + 让你知道` is retained as three observed songs in the dated record. They are explicitly optional in the active preparation list: optionality describes preparation choices, not whether the songs were actually performed on opening night.
- `(Live)` and `新专辑先行曲` remain performance annotations or aliases; they do not replace the canonical song titles.

## Content and privacy boundary

The preset contains only song metadata, empty lyric tracks, stage-order evidence, and source citations. All song and version ids are preserved so an existing private lyric library can be carried into the updated preset. No lyrics, original screenshots, private archives, or personal account identifiers are committed. The complete claim-to-source and raw-segment mapping is in `source-matrix.json`.
